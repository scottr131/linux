#!/bin/sh
# /etc/rc.d/rc.ceph-mgr - start/stop the Ceph mon daemon

# To change the default options, edit /etc/default/ceph-mgr.
if [ -r /etc/default/ceph ]; then
  . /etc/default/ceph
fi

if [ -r /etc/default/ceph-mgr ]; then
  . /etc/default/ceph-mgr
fi

start_ceph() {
  if [ ! -f /var/run/ceph/mgr.$CEPH_MGR_ID.pid ]; then
    mkdir -p /var/lib/ceph /var/log/ceph /var/run/ceph
    chown -R ceph:ceph /var/lib/ceph /var/log/ceph /var/run/ceph
    if [ -x /usr/bin/ceph-mgr ]; then
      echo "Starting Ceph Monitor:  /usr/bin/ceph-mgr -c $CEPH_CONF --setuser $CEPH_USER --setgroup $CEPH_GROUP -i $CEPH_MGR_ID --pid-file /var/run/ceph/mgr.$CEPH_MGR_ID.p
id"
      /usr/bin/ceph-mgr -c $CEPH_CONF --setuser $CEPH_USER --setgroup $CEPH_GROUP -i $CEPH_MGR_ID --pid-file /var/run/ceph/mgr.$CEPH_MGR_ID.pid
    fi
  fi
}

stop_ceph() {
  if [ -f /var/run/ceph/mgr.$CEPH_MGR_ID.pid ]; then
    echo "Stopping Ceph Monitor."
    /bin/kill -TERM "$(cat /var/run/ceph/mgr.$CEPH_MGR_ID.pid)"
    rm /var/run/ceph/mgr.$CEPH_MGR_ID.pid
  fi
}

restart_ceph() {
  stop_ceph
  sleep 1
  start_ceph
}

case "$1" in
'start')
  start_ceph
  ;;
'stop')
  stop_ceph
  ;;
'restart')
  restart_ceph
  ;;
*)
  echo "usage $0 start|stop|restart"
esac